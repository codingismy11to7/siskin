name: Prerelease Development Build

on:
  push:
    tags:
      - 'v*-dev*'  # Matches tags like v4.12.0-dev2, v1.2.3-dev-4, etc.

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - uses: actions/checkout@v3
        with:
          ref: development  # Still builds from development branch
          fetch-depth: 0

      - name: Cache Gradle and wrapper
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build 64-bit Debug APKs
        run: |
          bash ./gradlew assembleDebug

      - name: Sign Debug APK
        id: sign_debug
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/debug
          signingKeyBase64: ${{ secrets.KEYSTORE_BASE64 }}
          alias: ${{ secrets.KEY_ALIAS_GITHUB }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD_GITHUB }}
        env:
          BUILD_TOOLS_VERSION: "36.0.0"

      - name: Create Prerelease
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          body: |
            Development prerelease from commit ${{ github.sha }}
            
            **Note:** This is a prerelease and may be unstable. 
            **Branch:** development
            **Architecture:** 32/64-bit Universal apk
            **Build type:** Debug (signed with release keystore)
          draft: false
          prerelease: true
          files: |
            app/build/outputs/apk/debug/*-signed.apk
