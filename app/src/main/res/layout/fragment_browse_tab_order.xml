<?xml version="1.0" encoding="utf-8"?>
<!--
    The heading is centred and the activity's back button overlays it at
    start|top without an offset, the same arrangement fragment_plex_sign_in
    uses and for the reason recorded in activity_car_host: heading and
    content have to move together or not at all.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp">

    <TextView
        android:id="@+id/tab_order_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="16dp"
        android:text="@string/car_tab_order_title"
        android:textAppearance="@style/TextAppearance.Material3.HeadlineMedium" />

    <TextView
        android:id="@+id/tab_order_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="4dp"
        android:paddingBottom="12dp"
        android:text="@string/car_tab_order_hint"
        android:textAppearance="@style/TextAppearance.Material3.BodyMedium" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tab_order_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="16dp" />
</LinearLayout>
